---
allowed-tools: Bash(.agent/hooks/compile-context.sh:*), Read
description: Compile and display working context
---

Compile fresh working context and display it. Run these commands:

1. `.agent/hooks/compile-context.sh`
2. Read and display the contents of `.agent/working-context/current.md`
3. Also run `.agent/commands.sh recall failures` to show any known failures to avoid
