//! Re-export TypeHash from angelscript-compiler.
//!
//! The canonical implementation lives in `angelscript_compiler::types::type_hash`.
//! This module re-exports everything for backwards compatibility.

pub use angelscript_compiler::types::type_hash::*;
