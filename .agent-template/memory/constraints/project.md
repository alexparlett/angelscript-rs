---
category: constraints
active: true
---

# Project Constraints

Rust edition 2024, workspace with 6 crates, arena allocation with bumpalo, zero-copy AST with spans, TypeHash for type identity, two-pass compilation (registration then compile).
